__pycache__/
*.pyc

# secrets — API keys go here, never committed
.env

# per-device runtime config (recycling location, set from the app) — device-local
config.json

# large model weights (exceed GitHub's 100MB file limit)
model/pytorch_model.bin
model/model.onnx.data

# vendored pigpio library (source archive + extracted build tree)
v79.tar.gz
pigpio-79/
